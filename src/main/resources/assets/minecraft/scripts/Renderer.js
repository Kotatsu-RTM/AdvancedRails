var renderClass = "jp.ngt.rtm.render.RailPartsRenderer";
importPackage(Packages.com.github.kotatsu_rtm.advancedrails.router)

function init(modelSet, modelObject) {
    RendererRouter.init(renderer, modelObject);
}
