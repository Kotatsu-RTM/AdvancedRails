var renderClass = "jp.ngt.rtm.render.RailPartsRenderer";
importPackage(Packages.com.github.kotatsu_rtm.advancedrails.renderer)

function init(modelSet, modelObject) {
    RendererRouter.init(renderer, modelSet);
}
